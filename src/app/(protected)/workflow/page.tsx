import React from "react";

const page = () => {
	return (
		<main
			className={`container h-full overflow-auto grid grid-cols-1 gap-2 p-3`}
		>
			<div className={`w-full h-56 grid grid-cols-3 gap-3`}>
				<div className={`w-full h`}></div>
			</div>
		</main>
	);
};

export default page;
