// nodeTypes/index.ts
import { CustomNode } from './CustomNode';
import { DefaultNode } from './DefaultNode';

export const nodeTypes = {
  custom: CustomNode,
  default: DefaultNode,  // You can add more types here
};
